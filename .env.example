# Server environment
PORT=5050
# Base folder for resolving relative configPath and running promptfoo
BASE_DIR=.
# Where promptfoo writes its last run artifacts (relative to BASE_DIR if relative)
PROMPTFOO_LAST_RUN=.promptfoo/last-run

# Optional provider keys (uncomment as needed)
# NVAPI_KEY=
